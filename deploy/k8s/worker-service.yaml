apiVersion: v1
kind: Service
metadata:
  name: avatar-worker-metrics
  namespace: avatar
  labels:
    app: avatar-worker
    app.kubernetes.io/part-of: gophprofile
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: avatar-worker
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
