apiVersion: 1

providers:
  - name: avatar
    orgId: 1
    folder: Avatar Service
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
